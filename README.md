# 🚀 EvoSkill

> **会造工具的 AI 对话系统 —— 越用越聪明，越用越强大**

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

EvoSkill 是一个具备**自我进化能力**的智能对话系统。它不仅能通过调用现有 Skills 完成用户任务，更能根据对话中产生的需求，**自动设计、实现并部署新的 Skills**，让系统的能力随使用不断增长。

---

## ✨ 核心特性

### 🧠 智能任务执行
- **Skill 自动调度**：在对话中智能识别用户需求，自动调用最合适的 Skills 完成任务
- **多 Skill 协同**：支持多个 Skills 组合工作，完成复杂任务流程
- **上下文感知**：深入理解对话上下文，提供连贯、精准的服务

### 🔧 自我进化能力
- **需求自动感知**：从对话中自动提取潜在的能力缺口
- **Skill 自动生成**：基于需求自动设计 Skill 架构、编写代码、生成文档
- **一键部署上线**：自动完成 Skill 的测试、打包和部署流程
- **版本迭代管理**：支持 Skills 的版本控制和智能更新

### 🌱 持续学习成长
- **使用反馈闭环**：根据使用数据优化现有 Skills
- **能力图谱扩展**：技能网络随使用不断扩展和深化
- **个性化适配**：根据用户习惯自动调整行为模式

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                     应用层 (Chat UI / CLI / API)              │
└─────────────────────┬───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                  EvoSkill 核心引擎                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Agent Loop │  │  Session    │  │   Skill Evolution   │  │
│  │  (执行循环)  │  │  Manager    │  │   Engine (进化引擎) │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────┬───────────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
┌───────▼──────┐ ┌────▼────┐ ┌──────▼───────┐
│  Skill 仓库   │ │ Skill   │ │ 内置工具集    │
│  (已部署)     │ │ 运行时   │ │ (文件/网络)   │
└──────────────┘ └─────────┘ └──────────────┘
```

---

## 🚀 快速开始

### 环境要求

- Python >= 3.10
- 大模型 API Key（支持 OpenAI API 格式，如 Kimi、OpenRouter、SiliconFlow 等）

### 安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/evoskill.git
cd evoskill

# 安装依赖
pip install -e ".[dev]"

# 配置环境变量
export OPENAI_API_KEY=your-api-key
# 或
export EVOSKILL_API_KEY=your-api-key

# 支持任意 OpenAI API 兼容的后端：
# - Kimi API (api.moonshot.cn)
# - OpenRouter (openrouter.ai)
# - SiliconFlow (api.siliconflow.cn)
# - 或其他自定义 API 代理
```

### 启动对话

```bash
# 启动交互式对话
evoskill chat

# 指定工作目录
evoskill chat -w ./my-project
```

---

## 💡 使用示例

### 示例 1: 使用现有 Skill

```
User: 帮我查一下北京明天的天气

EvoSkill: [自动调用 weather_skill]
北京当前天气：晴，温度 25°C，湿度 45%
```

### 示例 2: 触发 Skill 自动生成

```
User: 我有一批产品图片，想把它们转换成素描风格，
      并且加上水印文字

EvoSkill: 我注意到您需要图片风格转换和水印添加功能。
          这可以通过一个新的 image_processor Skill 来实现。
          
          [正在生成 Skill...]
          ✓ 架构设计完成
          ✓ 代码编写完成
          ✓ 测试用例通过
          ✓ 部署完成
          
          Skill "image_processor" 已成功上线！
          现在我可以帮您处理图片了，请上传图片文件。
```

### 示例 3: 代码助手

```
User: 帮我把这个函数改成异步的

def fetch_data(url):
    import requests
    return requests.get(url).json()

EvoSkill: [查看代码] → [编辑代码]
已将函数改为异步版本：

async def fetch_data(url):
    import aiohttp
    async with aiohttp.ClientSession() as session:
        async with session.get(url) as resp:
            return await resp.json()
```

---

## 📁 项目结构

```
evoskill/
├── evoskill/               # 核心代码
│   ├── core/               # 核心引擎
│   │   ├── types.py        # 类型定义
│   │   ├── session.py      # Agent 会话
│   │   ├── events.py       # 事件系统
│   │   └── llm.py          # LLM 接口
│   ├── skills/             # Skills 系统
│   │   ├── loader.py       # Skill 加载器
│   │   └── builtin.py      # 内置工具集
│   ├── evolution/          # Skill 进化引擎
│   │   ├── engine.py       # 进化主引擎
│   │   ├── analyzer.py     # 需求分析
│   │   └── generator.py    # 代码生成
│   └── cli/                # 命令行界面
│       └── main.py         # CLI 入口
├── skills/                 # 用户 Skills 目录（自动管理）
├── docs/                   # 文档
│   ├── ARCHITECTURE.md     # 系统架构
│   └── PROMPTS.md          # 提示词设计
├── tests/                  # 测试代码
└── examples/               # 示例
    └── skills/             # 示例 Skills
```

---

## 🔧 创建自定义 Skill

### 方式 1: 使用 CLI 创建模板

```bash
# 创建新 Skill
evoskill create my-skill -d "我的自定义 Skill"

# 编辑生成的文件
cd skills/my-skill
vim SKILL.md
vim main.py

# 测试
pytest tests/
```

### 方式 2: 手动创建

```
my-skill/
├── SKILL.md          # Skill 定义文件
├── main.py           # 实现代码
└── tests/
    └── test_main.py  # 测试
```

**SKILL.md 示例**:
```yaml
---
name: my-skill
description: 描述你的 Skill
tools:
  - name: my_tool
    description: 工具描述
    parameters:
      input:
        type: string
        required: true
---

# My Skill

详细说明...
```

**main.py 示例**:
```python
async def my_tool(input: str) -> str:
    """工具实现"""
    return f"处理了: {input}"
```

---

## 🛠️ 开发计划

- [x] 项目初始化与架构设计
- [x] 核心对话引擎实现
- [x] Skill 调度与执行框架
- [x] 内置基础 Skills（文件操作、网络请求、数据处理等）
- [x] Skill 进化引擎 v1.0
  - [x] 需求提取与形式化
  - [x] Skill 代码自动生成
  - [x] 自动化测试验证
- [ ] Web UI 界面
- [ ] Skill 市场与分享机制
- [ ] 多模态支持（图片、音频、视频）

---

## 🏛️ 架构设计

详细的架构文档请参考 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

系统核心设计参考了以下优秀开源项目：

| 项目 | 学习要点 |
|------|---------|
| [Pi Agent](https://github.com/can1357/oh-my-pi) | Agent 核心架构、会话管理 |
| [OpenClaw](https://github.com/openclaw/openclaw) | 消息渠道集成、Skill 系统 |
| [Goose](https://github.com/block/goose) | MCP 协议、工具生态 |
| [Aider](https://github.com/Aider-AI/aider) | 代码编辑、RepoMap |

---

## 🤝 参与贡献

我们欢迎所有形式的贡献！无论是提交 Bug 报告、功能建议，还是代码贡献，都能帮助 EvoSkill 变得更好。

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送分支 (`git push origin feature/amazing`)
5. 创建 Pull Request

详细指南请参考 [CONTRIBUTING.md](CONTRIBUTING.md)

---

## 📄 许可证

本项目采用 [MIT](LICENSE) 许可证开源。

---

## 🙏 致谢

- [Claude](https://claude.ai/) - 提供强大的 AI 能力支撑
- [Pi Agent](https://github.com/can1357/oh-my-pi) - 优秀的 Agent 架构设计
- [Anthropic Agent Skills](https://agentskills.io/) - 开放的 Skill 标准

---

<p align="center">
  <sub>Built with ❤️ by EvoSkill Team</sub>
</p>
